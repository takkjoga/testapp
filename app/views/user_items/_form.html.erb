<%= form_for @user_item, :html => { :class => "form-horizontal user_item" } do |f| %>

  <% if @user_item.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@user_item.errors.count, "error") %> prohibited this user_item from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @user_item.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :user_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:user_id) %>
  </div>
  <div class="form-group">
    <%= f.label :item_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :item_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:item_id) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                user_items_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
